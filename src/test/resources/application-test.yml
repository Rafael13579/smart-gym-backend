spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:



  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

jwt:
  secret: minha-chave-secreta-super-segura-com-mais-de-32-caracteres
  expiration: 3600000

app:
  upload:
    dir: ./uploads
    base-url: http://localhost:8080/api/images/exercises/

gemini:
  api-key: test-api-key
  api:
    url: https://generativelanguage.googleapis.com
